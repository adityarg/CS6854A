Run "assign2.sh" to run all scripts.

"fullcode.py" contains code of all parts.

Following are the libraries needed to be present for code to run successfully : 'csv','numpy','pandas','urllib','sys','re'
(All libraries are imported in the code)

This zip files also contain an 'ipynb' file that can be opened in Jupyter Notebook to see the full code.

All output files with appropriate names will be generated by running the script.